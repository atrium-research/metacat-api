VERSION=0.0.0

# Path read by the json store (metacat-data JSON)
JSON_DATA_DIR=example_data

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log format: json (production) or console (development)
LOG_FORMAT=console

API_KEYS=Key1,Key2

GIT_USERNAME=pusher
GIT_PASSWORD=ghp_abc123