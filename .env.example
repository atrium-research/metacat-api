VERSION=0.0.0

# Path read by the json store (metacat-data JSON snapshots)
JSON_DATA_DIR=example_data

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log format: json (production) or console (development)
LOG_FORMAT=console

API_KEYS=Key1,Key2